export const ROUTES = {
  HOME: '/home',
  REGISTER: '/register',
  LOGIN: '/login',
}

export const STORAGE_KEYS = {
  TOKEN: 'token',
}

export type NotificationType = 'success' | 'error' | 'warning' | 'info'
